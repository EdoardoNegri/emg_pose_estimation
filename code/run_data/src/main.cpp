#include "process.h"

int main() {
    // initialize EMG data acquisition
    // load the trained model
    initialize_parameters();
    // initialize visualization tools
    while(true) {
        // get data from EMG
        // preprocess that data
        // run the model
        // postprocess the model output
        // visualize the result
    }
    return 0;
}
